using System.Windows.Controls;

namespace MiddlewareApp.Views;

/// <summary>Centered spinner shown while checking for a persisted session (spec §6.5).</summary>
public partial class BootScreen : UserControl
{
    public BootScreen()
    {
        InitializeComponent();
    }
}
